{
  "name": "plugin-marketplace",
  "version": "1.0.0",
  "description": "Marketplace de plugins con soporte para Node.js y despliegue en Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test src/**/*.test.js",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
